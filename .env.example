# Copy to .env and fill in. Both values are safe to expose in the client bundle
# (the anon key is a publishable JWT — Supabase Row Level Security enforces real auth).
VITE_MASTER_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_MASTER_SUPABASE_ANON_KEY=YOUR-ANON-KEY
